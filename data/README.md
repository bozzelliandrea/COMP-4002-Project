# Data Directory

Place your datasets here. Supported formats include CSV, JSON, and Parquet.

The notebook uses the built-in **Iris** and **Breast Cancer** datasets from `scikit-learn`
by default, so no external files are required to get started.

When you add your own dataset, update the data-loading cell in `notebooks/main.ipynb`
to point at the appropriate file path.
